print ('привет! выберем порядковые номера значений, дающие в сумме целевое значение:')
z = [10,9,14,23,15,0,0]
print ('массив - ', z)
s = int (input('введите целевое значение:'))
a = 0
while a != len(z) - 1:
    b = a
    while b != len(z) - 1:
        b += 1
        if z[a] + z[b] == s:
           print ('первое - ', a , 'второе-', b)
           a += 1
    a += 1
print ('Итог z=', z)
